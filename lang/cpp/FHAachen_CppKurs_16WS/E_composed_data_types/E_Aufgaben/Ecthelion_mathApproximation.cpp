/*
 * FH Aachen Cpp 16WS
 * Aufgabe Duilin
 * Autor: Johannes Wasmer
 * Datum: 06.07.2017
 * 
 * Aufgabe:  Schreiben Sie ein Programm, welches die angegebene Berechnungs-
 * vorschrift ausführt, bis sich der neue Wert nicht mehr “signifikant”
 * vom vorhergehenden Wert unterscheidet (z.B. Abstand 1e-12). Die
 * Vorschrift lautet:
 * x n+1 =1/2 cos(x n ) Startwert x 0 = 1.0
 * • Wie lautet der Wert und wie viele Iterationen werden benötigt?
 * 
 * 
 * Themen: Mathematik
 */

#if defined(_WIN32) || (defined(__APPLE__) && defined(__MACH__))
#include <cstdio>
#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <algorithm>
#include <functional> //for Lambdas
#include <memory>
#include <type_traits>
#include <initializer_list> //for AutoDeclType
#else
#include <bits/stdc++.h>
#endif

using namespace std;

int main() {

    return 0;
}