/*
 * FH Aachen Cpp 16WS
 * Aufgabe Duilin
 * Autor: Johannes Wasmer
 * Datum: 06.07.2017
 * 
 * Aufgabe: Implementieren und Testen Sie eine Struktur, die ein Polynom beschreibt.
 * Das Polynom ist durch seine (double) Koeffizienten eindeutig beschrieben.
 * Nehmen Sie an, dass es zunächst nicht mehr als Grad 9 hat.
 * Programmieren Sie Funktionen, um
 * • ein Polynom auszuwerten (eval) und
 * • zu einem weiteren Polynom zu addieren (add).
 * 
 * 
 * Themen: struct, arrays
 */

#if defined(_WIN32) || (defined(__APPLE__) && defined(__MACH__))
#include <cstdio>
#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <algorithm>
#include <functional> //for Lambdas
#include <memory>
#include <type_traits>
#include <initializer_list> //for AutoDeclType
#else
#include <bits/stdc++.h>
#endif

using namespace std;

int main() {

    return 0;
}